{
  "name": "code-review-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for AI-powered multi-dimensional code review — works in VSCode, Cursor, IntelliJ, Claude Desktop",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublish": "npm run build"
  },
  "bin": {
    "code-review-mcp": "dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.55.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
